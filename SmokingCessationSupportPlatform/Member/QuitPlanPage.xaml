<Page x:Class="SmokingCessationSupportPlatform.Member.QuitPlanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="QuitPlanPage">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="My Quit Plans" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <Button Content="Load My Plans" Width="150" Click="BtnLoadMyPlans_Click" Margin="0,0,0,10"/>
            <DataGrid x:Name="dgQuitPlans" AutoGenerateColumns="False" Height="300" Width="832">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Plan Name" Binding="{Binding PlanName}" Width="*"/>
                    <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate}" Width="100"/>
                    <DataGridTextColumn Header="End Date" Binding="{Binding EndDate}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
