<Page x:Class="SmokingCessationSupportPlatform.Member.AchievementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="AchievementPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="🎖 Huy hiệu của bạn" FontSize="26" FontWeight="Bold" Foreground="#2E7D32" Margin="0 0 0 10"/>

            <!-- Bộ lọc loại huy hiệu -->
            <ComboBox x:Name="TypeFilter" Width="200" SelectionChanged="TypeFilter_SelectionChanged" Margin="0 0 0 20">
                <ComboBoxItem Content="Tất cả" IsSelected="True"/>
                <ComboBoxItem Content="HEALTH"/>
                <ComboBoxItem Content="FINANCE"/>
                <ComboBoxItem Content="MOTIVATION"/>
            </ComboBox>

            <!-- Panel hiển thị huy hiệu -->
            <WrapPanel x:Name="BadgePanel" ItemWidth="200" ItemHeight="150" Orientation="Horizontal"/>
        </StackPanel>
    </ScrollViewer>
</Page>
